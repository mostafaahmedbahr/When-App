
import '../../../../../main_importants.dart';

class TimerCount extends StatelessWidget {
  const TimerCount({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("00.59",style: AppStyles.gray16Medium,));
  }
}
